1.
SELECT NAME, MAX(PRICE) AS MAX_PRICE FROM MEAL
WHERE CALORIES > 500
GROUP BY NAME
ORDER BY  MAX_PRICE DESC;

2.
SELECT NAME, AVG(PRICE) AS AVG_PRICE 
FROM meal #AVG=AVARAGE
WHERE PRICE > 15
GROUP BY NAME

3.
SELECT NAME, PRICE AS MAX_PRICE
FROM MEAL
HAVING PRICE = (SELECT MAX(PRICE) FROM meal);

4.
SELECT NAME, PRICE AS AVARAGE_PRICE
FROM meal
HAVING PRICE < (SELECT AVG(PRICE) FROM meal);

5.
SELECT NAME,PRICE,CALORIES 
FROM meal
HAVING PRICE < (SELECT AVG(PRICE) FROM meal) 
AND CALORIES > 600;
